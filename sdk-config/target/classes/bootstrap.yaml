spring:
  cloud:
    nacos:
      server-addr: localhost:8848   # nacos地址
      config:
        file-extension: yml        #文件后缀名  对应nacos配置文件的Data Id的后缀名
  profiles:
    active: dev          #开发环境，对应nacos配置文件的Data Id的profile名