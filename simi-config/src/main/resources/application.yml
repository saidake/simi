#=============================================================================================== Log Configuration
# simi:
#  log-path: ${PROJECT_PATH}/AAAlogs
logging:
  config: classpath:logback-simi.xml      # Specifies the logback configuration file (default is logback-spring.xml)
  level:
    root: info
    org.mybatis: debug
    java.sql: debug
    org.springframework.web: debug
